import styled from '@emotion/styled'

import { ViewProps } from '../elements.types'
import StyledView from '../view'

const Grid = styled(StyledView)<ViewProps>({ display: 'grid' })

export default Grid
