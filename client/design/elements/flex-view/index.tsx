import styled from '@emotion/styled'

import { ViewProps } from '../elements.types'
import StyledView from '../view'

const Flex = styled(StyledView)<ViewProps>({ display: 'flex' })

export default Flex
