export const breakpoints = ['4em', '48em', '64em', '80em'];

export const breakpointsAliases = {
  sm: breakpoints[0],
  md: breakpoints[1],
  lg: breakpoints[2],
  xl: breakpoints[3],
};

export type Breakpoints = typeof breakpoints;
